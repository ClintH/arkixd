---
export interface Props {
  src: string;
  base: string;
}
let { src, base, alt } = Astro.props;

if (!base.startsWith('/')) {
  //base = '/images/' + base;
  base = '/assets/2021/';
}

const path = base + src;
//console.log(`path: ${path} base: ${base} src: ${src}`);
---

<img src={path} />
