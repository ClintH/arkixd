export interface ProjectMetadata {
  subTitle?: string;
  title: string;
  creators: string;
  course?: string;
  year: string;
  yearLevel: string;
  tags: string[];
  image: string;
  imageBase: string;
}
